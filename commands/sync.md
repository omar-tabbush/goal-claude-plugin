---
description: Record this session's decisions and rebuild the dashboard (the Stop hook does this automatically)
---

Follow `${CLAUDE_PLUGIN_ROOT}/docs/SYNC.md` exactly for the current project.

No `.compass/` here? Say `compass: not initialised` and stop.
